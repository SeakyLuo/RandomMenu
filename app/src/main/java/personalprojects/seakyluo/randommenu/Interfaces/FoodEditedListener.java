package personalprojects.seakyluo.randommenu.interfaces;

import personalprojects.seakyluo.randommenu.models.SelfFood;

public interface FoodEditedListener{
    void FoodEdited(SelfFood before, SelfFood after);
}
